# MagicDesk — 本机 Mac 版 0.2.0

双击 `dist/macos/MagicDesk.app`。备份使用 `dist/MagicDesk-macOS-arm64.zip`，解压后得到同一个 App。运行无需安装 Godot、Blender 或联网。原 `HELIOS.app` 与原 Blender 源文件保留作基准。

本版专为当前 Apple M5 电脑生成，使用原生 arm64 和 Metal 渲染，不包含 Intel 架构。包含 B 孵日器，以及首批 F、G、I、J、K、L、M、N 八款。材质和视觉表现处于持续制作检查中，功能通过不等于 AAA 美术验收。

点击底座左前上沿的金属压条，或按 Tab，展开机械铭牌。点击铭牌直接切换；滚轮或拖动索引滚轴浏览后面的装置，没有左右箭头。Esc 收起档案；资源准备阶段按 Esc 取消切换。每张牌内的缩略图来自实际三维资产。

| 底座按钮 / 快捷键 | 功能 |
| --- | --- |
| 1 | 唤醒 / 休眠 |
| 2 | 当前装置主动作 / 恢复；M 为公转暂停 |
| 3 | 当前装置招牌动作；M 星噬、N 探针穿越 |
| 4 | 零件拆解 |
| 5 | 组装 / 收拢；M 在组装状态下重构行星 |
| 6 | 上部旋转 / 暂停；N 为相位校准 |
| 空格 | 上部旋转 / 暂停 |
| 7 / 红色 X | 收拢、熄灯并退出 |

各装置使用自己的实体控制器外形；鼠标悬停可查看动作名称。拖动底座移动摆件，拖动上部旋转模型。右键打开动作、静音、置顶、重置位置和退出菜单。快捷键在 App 获得焦点时生效。Cmd-Q 和系统退出请求也经过收拢熄灯流程。

Mac 版通过 Godot 的 Cocoa 透明窗口直接呈现 Metal 实时画面；使用固定透明画布保留轨道粒子和蒸汽空间。透明空隙按真实模型表面射线检测穿透鼠标；特效光晕本身不拦截桌面点击。支持中文悬浮提示。

当前使用本地 ad-hoc 签名，未进行 Developer ID 签名或 Apple 公证。适合本机运行与测试，不表示完成公开分发认证；经网络下载到另一台 Mac 时可能被 Gatekeeper 拦截。

从源码重建：

```sh
bash tools/build_macos.sh
```

需要 Godot 4.7.1 stable 及同版本 Mac 导出模板。默认使用 `/Applications/Godot.app/Contents/MacOS/Godot`，可通过 `GODOT_BIN` 覆盖路径。
