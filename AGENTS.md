# MagicDesk 制作约定

- 2026-09-10 换机交接优先读 HANDOFF 顶部。新独立特效分镜和单张纹理在 `production/G_record_player/r3_fx/`：吸附、读取、构建、回收。AI形态在 r2。这两轮都还未接入模型/EXE；旧爪式试制仅是功能基线。继续做G，不推进下一款；不要因Git提交或技术测试通过把它称为成品。

- 最新形态追加：爪子改为机械臂末端的球形 AI 快门机头，带镜内像素表情，独立下方投射器以光束吸取原唱片，待机可眨眼/歪头。新目标、有效图与弃稿标记见 `production/G_record_player/r2/ART_AND_BEHAVIOR.md`；先按此推进，不继续精修旧爪式结构。新AI、加大尺寸与新特效尚未做进EXE。

- 用户最新明确拒绝新唱机首轮的整体质量：上部太小、唱片没有铺满小唱盘、打印特效与图稿不符，远未达到期待。优先重做比例、模型工艺与完整特效分镜，并制作对应素材。`native_console` 的输入测试只证明去掉 UI 后控件可用，不能作为视觉验收。所有操作留在实体操控台；不得恢复黑色悬浮操作面板或用提示遮挡控件。

- 最新用户方向已取消大块方形书壳：围绕六片黑色档案盘，做原子之心风格的自动取放、读取唱机。暂停旧书模型交付，先看 HANDOFF 顶部与 `production/G_record_player/`。唱片必须实际取出、落盘、扫描、退臂后才显示藏品；归片路径也要完整。

- 用户最新拒绝 G 的视觉和玩法；当前是“自动换碟式藏品书”返工，优先读 HANDOFF 顶部与 `production/G_archive/VISUAL_REWORK.md`。必须先验证书的翻页定位、读取、展台和藏品出入场顺序；不要推进 I，不要以通过测试冒充图稿级精修完成。

- 当前已继续完成 G 专项制作流程，来源在 `production/G_complete/`、`G_complete.blend` 与对应 GLB，运行时专用 `g_instrument/g_visuals`。新轮次先看 HANDOFF 顶部与 G 实机反馈；下一款是 I，不要重复从旧 G 灰模开始。测试通过不能代替用户的视觉验收。

- 最新用户优先级：先把 F 的物理、操作手感、模型和完整特效做好，再做下一款；不要凭基础接口/测试通过宣称达到 AAA。F 的最新来源在 `production/F_complete/`、`F_complete.blend` 与对应 GLB。新轮次先读 HANDOFF 顶部，旧 F/F_refined 保留。

- 2026-09-09 用户最新决定：只使用订阅自带的内置生图。图稿与视觉资产使用内置 `image_gen`；不要调用、查询、探测或使用任何 apilink 服务或外部图片 API。不得把内置工具的输出声称为已指定某个不可验证的底层模型。

- 跨设备接手先读 `HANDOFF.md` 顶部最新更新。Windows 0.2.2 EXE 已沿实体入口回放 M/N 接入；Mac 尚未验证本次版本。除 B 外逐款精修，不能用输入测试代替视觉验收；共用入口是明显的奶白弧形舱罩，不是薄盖板。

- 先读取 `concepts/production_selection.json` 和 `PRODUCTION_ART.md`，以用户最新确认的范围为准。画廊候选不自动等于首批制作清单。
- 所有装置使用一份共享底座。真实尺寸来自 `app/assets/helios_model.glb` 的 `BASE_FIXED` 与 `concepts/shared_base/底座尺寸契约.md`，不得依照生成图为不同装置分别改变底座外径、高度或层数。
- 美术延续《原子之心》参考的复古未来工业、仿生机械、奶白瓷壳、镍铬骨架与克制的红色细节。各装置可用不同数量、形状和行为的实体控制器，但须适配同一操作匣区域。
- 用户明确要求：每款最终选定装置，涉及动作或特效时，必须先出图设计，再制作对应视觉资产。正式效果不能纯依靠 Godot 程序图元或临时粒子凑出；Godot 负责交互、机构驱动和实时合成。
- 灰模用于验证结构、空间和动作，不得把灰模或纯程序占位效果宣称为正式美术完成。已确认范围内的常规迭代自主完成，不因此新增逐步审批。
- 已选定切换的机械伸出结构：环沿隐藏触发、折叠连杆、实体铭牌。入口、牌架与模型交接必须在同一真实底座和相机上验证。
- 不重跑会覆盖当前 Blender 人工调整的早期建模脚本；先检查其修改范围。保留 HELIOS 既有模型与可运行版本。
