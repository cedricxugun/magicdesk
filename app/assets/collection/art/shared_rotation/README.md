# 共用旋转控件专属图形

由 `production/shared_rotation/concept_v2.png` 的设计方向精确绘制成 SVG：分段外环、旋转、暂停、点击回波和弱投射细线。保持无背景透明，供同一隐藏发射口的实时合成。不是生成图裁剪，也不是已完成的运行时入口。

普通状态约28–34逻辑点，交互区单独扩大。实际位置、底座玻璃窗、显影/压缩/回弹动画和与关闭键的间隔待实机实现验证。
