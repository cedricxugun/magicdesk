# 未通过的原生预览

该目录不能作为整机或动画通过的证据。

首次有主窗口hide调用错误和绝对core_report路径拼接错误；第二次发现驱动函数ease与内置函数重名；第三次实际暴露body导出缺六片顶层活动瓷甲。第三次虽产生state_*.png和render_review.json，bind已断言失败，panels为空，不能使用其播放截图证明完整六片开合。

当前修复将六个活动根节点归入R82_COIL_ROOT，并验证改变层级不会移动原姿态。后续使用新的installed_r3与native_r3证据，不覆盖这里的失败记录。
