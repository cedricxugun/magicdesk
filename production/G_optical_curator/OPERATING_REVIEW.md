# 读取、构建与回收检查

2026-09-10，在新灯光与光学材质下重新拍摄实际运行状态。当前结果在 `review/G_optical_curator/operating/`，原结果在其 `before_flow_fix/`。正常图使用固定App镜头；文件名带 `_detail` 的图另用检查镜头。

本轮修正：

- 原先回收时实体在减少，但微光仍向藏品输送。现在使用连续的流动相位，回收时改变推进方向，光点和沿线光纹退回原唱片；反转时不重设相位，避免突然跳位。
- 原先落盘释放只整体变淡。现在实体线段和三维场体从唱片端向投射器缩回，盘面接点先熄灭；承载阶段保持完整光场。
- 建立光场时，接点、细丝、薄场体分段出现。预热仍覆盖所有实际使用的图层。
- 检查脚本增加独立输出目录，避免短检查覆盖完整源动画 take；超时会计入失败，不再只输出日志。

`flow_qa.json` 验证实际微光实例的位置、反转连续性、释放几何缩短和安静终点。`flow_only_mechanical_fingerprint.json` 对比本轮1080帧中的机械姿态和状态，前后完全相同；这一步未改原片所有权、尺度或机构动作，不能将此指纹用于之后独立的读头修正。GPU流光仍属于运行时合成，未假称烘焙进已有Blender动画。

待继续：观测塔的实体造型仍明显过于简化，拱廊、穹顶、内部机构和楼梯与目标有差距；当前构建线也不够连续，供能线仍偏硬。下一步按 `observatory_r2/MODELING_NOTES.md` 建立更准确的组件源，再修它对应的材质、结构线与演出。此轮只修正具体行为错误，没有把G或全部特效标记为完成。

## 读取净空的独立修正

之后的实际网格检查又发现读头外圈会穿入唱片，旧读取原点1.012下的最小净空为-0.00323。将读取原点改为1.030后，13个扫描半径上的实际表面射线检查得到最小净空约0.01477；最高位置的活塞套筒搭接仍约0.050。见 `reader_clearance_before.json`、`reader_clearance.json` 与 `connection_report.json`。

已重新记录完整3805帧实际运行 take；除 `GR_Tonearm` 的高度外，其他所有对象姿态与状态完全相同，见 `reader_other_motion_fingerprint.json`。`sync_curator_reader_motion.py` 先备份现有源，再只修正读头的379个高度关键帧。保存后的源通过5250个姿态比对，无缺失纹理；没有重新生成或覆盖整套模型。

转运检查扩展到读取臂，以及评估后的曲线和文字几何。结果以匹配当前模型/元数据散列的报告为准。旧截图和旧报告中的-0.00323净空是已修正问题的基线，不是当前通过值。
