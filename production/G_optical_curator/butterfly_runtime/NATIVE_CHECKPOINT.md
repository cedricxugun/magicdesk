# G1 原生检查与主开发资源更新 · 2026-09-11

新蝶已在独立 arm64 `dist/macos-review/MagicDesk-G1-review.app` 检查，并以开发检查点纳入主 G。它不是整 G 完成或 AAA 验收。旧主 GLB、JSON 和 Blender 源保存在 `blender/collection/checkpoints/before-butterfly-5c6315708275/`。旧 `dist/macos/MagicDesk.app` 没有覆盖。

## 可追溯证据

- `review/G_optical_curator/butterfly_r2/native/build.json`：独立包、PCK、二进制和原注册表散列。
- `native/native_input_report.json`：原生实体滑块拖动、滚轮、动作登记/松手归零、换片归位，以及本次 G1 重新读取/构建和完成后取消。
- `native/packed_checks/candidate_qa.json`：同一已导出 PCK，由匹配版本 Godot 编辑器无窗口加载进行十项场景/驱动回放；包含构建中取消，不属于原生 UI 验证。
- `promotion.json`：主 GLB 和源逐字节等于被检候选；主 JSON 仅改源路径和开发状态说明，运行字段完全等价。
- `promoted/candidate_qa.json`：重新导入后的主 G 十项回放通过。

原生实际拖动将蝶翼参数从 0.8 调到 0.523076923，再通过滚轮到 0.568076923。换片将蝶唱片归回自身片槽后载入第 2 件。持续按住蓄力的非零峰值没有被本次原生记录捕获，不能把动作登记替代长按峰值验证。场景回放覆盖了长按。

构建中途取消的原生尝试晚于构建完成，只记录到先收翼再回收；保留这项覆盖缺口。之后牌架收回到停机态的重新操作也不计入通过项，待整体 G 的首次使用流程复查。

## 帧时与可见性边界

连续逐帧记录采用单调时钟，未启用电影固定步长。只分析恢复操作后的限定窗口，排除无人操作的整夜记录。G1 playing 的 45954 帧中位 17.863 ms，P95 22.683 ms，最大 337.55 ms；包含 CUA 截图/系统调度，未证明稳定 60 FPS。Metal GPU 时间字段始终为 0，视为不可用。实体小字仍偏小，完整演出及全机材质/性能还要统一复查。

两次独立原生会话均正常 Cmd-Q 退出；没有把用户在中断期间自行选择 G1/F/G 计作代理操作。原始 profiling 流与命令渠道只在本机忽略目录保留，精简的事件、参数和构建散列进入报告。

## 接续

继续 G2 潮汐帆船。G0/G1 保留为第一轮开发结果，在整 G 收尾复查上面的限制；G2–G5 仍待精修。不要重复 G1 四连杆、封合遮罩和整段动画烘焙。后续组件从现在已包含新蝶的主 G 派生，不从旧主源重新开始。
