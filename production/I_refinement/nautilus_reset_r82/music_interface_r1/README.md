# R83：月光口部与光学接口（继续制作中）

**最新实际源是 `I_r82_music_receiver_r4.blend / c1942bd1`，报告 `installed_r5/build.json`，上部组件 `I_r82_music_body_r5.glb`。有效实际Godot组合图在 `native_r6/`。下方r2路径是过程起点，不能作为当前导出。**

本轮还修了两个真实导出问题：六个活动瓷甲根原本未归入上部根，现已归入且12姿态世界矩阵差0；静态修改器原本未导出，119物件的实际图元数量因此少于Blender求值结果。改成求值导出后，又发现旧内卷金属层的恒定内缩使小卷处塌成重叠薄面，已用始终为正的平滑壁厚重建，再开真实导向/接收孔。新内层流形、正体积、无所查退化面。

`installed_r5/export_geometry_check.json`对217个上部物件核对独立Blender求值图元数和抽样顶点，全部对应、抽样最大误差0；这不是完整有向三角/法线/连续碰撞或AAA验收。`installed_r5/installed_contact_check.json`覆盖217体部零件对421实体口部/投影器零件的13工作/正负压力姿态，91个接触按严格前后平面确认为安装座贴合，没有所查意外相交；体部彼此、自接触和全部连续动作仍不在这个范围。

实际Godot的闭合、开启和第一乐章连续片段已在`native_r6`渲染，冷态预热与完整六片驱动已接入独立评审脚本。使用较暖的灯光、高质量空间阴影与SSAO、8x MSAA及投影器局部反射光，仍是评审参数，未改主App灯光。灯光/动作/渲染结果依然没有达到整机最终艺术验收；12内声腔、更多工艺/材质、实体控件、完整拆装和正式集成仍未完成。

用户的完整AAA目标仍active，I未完成，后续款没有因此缩减。上轮是实际进展，不是等待：已建立新资产、改动运行时并获得新/旧接口和自然三乐章的证据。

## 当前权威路径

- 整机可编辑候选：`blender/collection/I_r82_music_receiver_r4.blend`，报告：`review/I_refinement/nautilus_reset_r82/music_interface_r1/installed_r5/`。r2的Cycles图只用于此前投影位置/组合过程比较。
- 上部独立组件：`app/assets/collection/components/I_r82_music_body_r5.glb`，不含第二份底座，尚未主App集成/整体验收。
- 功能内芯与新镜头源：`blender/collection/I_r82_acoustic_optics_r2.blend`，组件`I_r82_acoustic_optics_r2.glb`；报告`review/I_refinement/nautilus_reset_r82/music_interface_r1/optical_core_build_r2.json`。
- 光学布局/组件：`app/assets/collection/art/I/r82_music_interface/r2/layout.json`与`optics.glb`。
- 原R9机构源仍保留，不能把其局部碰撞通过自动套到新接收座。正式registry仍R68，没有新EXE或Git提交/推送。

## 实际完成的内容

剥离了旧白色外圈及其旧卡扣，共104个外部对象；保留373个功能网格的几何、原UV、形态键与原始局部/世界变换。三片喉闸的六片薄箔、所有运动数据、膜片/弹簧、原时钟与完整三乐章保留。旧壳内通用黑色内壁被真实内芯壳体和新短过渡替代；固定金属背板/衬层开出接收空间，并有接收环、六安装座/引导衬套/保留螺钉。螺纹细节与全部接触尚未完成检查，不能称可制造成品。

三枚新的球铰读谱头在实际旧端头的拟合座上安装，镜片有真实几何；运行时对准中央读谱点，并按真实膜片位置移动光束起点。谱面维持原约.84×.504的世界阅读范围，朝默认观察方向展开，空白完全透明。R1纯沿Y前移造成透视右偏；R2沿真实相机方向校正中心。Blender静态图取真实谱页第4四分音符位置作空间展示，不是音频演奏证明。

内置生图`interface_concept_r1.png`与`PROMPT.txt`已保留；图中出现的玻璃矩形边框不采用。制作只使用真实透明谱线/音符，没有黑色屏幕或外框。

## 运行时变更与已获得的证据

`i_moonlight_staff.gd`新增可选的现有镜片/球铰绑定、实际读取法线、退出时恢复球铰姿态；原帽盖路径保留。`i_moonlight_staff.gdshader`新增可选的沿真实谱面竖轴显影，默认仍使用旧Z轴行为。

- `runtime_qa_r2.json`：新组件实际加载六片薄箔数据；ready后播放、透明谱面、前后拾取、暂停冻结、跨三个乐章seek、恢复、淡出/收谱/合闸通过。九组镜片表面/光束端点/朝向检查通过，另确认镜片真实发生位移。Dummy音频，不是扬声器或完整整机验收。
- `legacy_runtime_qa.json`：按当前主App的旧口部/光学配置复测，通过上述播放/控制范围，旧接口兼容。
- `full_work_r2/result.json`：新内芯自然走完完整三个乐章，无暂停/seek，播放时长约961.69秒，最终音源位置961.036秒，193条状态样本。仍不证明逐音校准、扬声器、原生画面或整机质量。初次harness目录错误已修复并重新完整运行；失败日志独立保留，不能将它算通过。
- `tangent_audit_r2.json`：新内芯所有使用法线贴图的glTF图元均有切线，六片运动薄箔均带原始TANGENT。新内芯/光学导入关闭自动LOD、shadow proxy、压缩和再生切线，避免破坏源数据。

## 接下来必须做

1. 把当前正确上部组件做Godot导入姿态/实际网格见证，补连续、反向与实体操控检查；当前有效原生图不等于整机已经验收。
2. 继续体部内部、固定口缘/过渡与移动瓷甲、完整拆装的接合/穿插检查；已完成的体部对内芯13姿态不能覆盖这些。
3. 当前源已把光束预览改为透明/Emission混合，不能再使用r2的残留基色版本；仍需完整进入、暂停、关闭/退出的实际表现核对。
4. 保持谱面真正居中且没有被壳遮住。用Godot真实采样和同相机图核对，不能只看大图或把完整纹理压扁来伪造可读性。
5. 新卷体内部仍缺完整12声腔/膜片/导光；按已设计的机械形态补齐并接真实音乐三频段响应，再完善工艺材质、光效、完整拆装、实体控件和主App/EXE。

不要跳到下一款以回避I的艺术/功能缺口。所有其余目标与原队列保留，只有完成对应真实交付后才推进/标完成。

失败记录：`native_r2/INVALID.md`标明窗口/路径、函数重名及漏导瓷甲的问题；`native_r3`虽有正常运行但沿用漏修改器的旧组件；`native_r4`影子过硬不是最终灯光；`native_r5`首次运行时导入配置尚未正确应用，不作为导入保真证据。正确组件与配置从`installed_r5`和`native_r6`接续。
