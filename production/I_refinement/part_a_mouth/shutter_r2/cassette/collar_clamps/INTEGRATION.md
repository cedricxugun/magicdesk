# 新卡扣已接入当前口部原生预览

本机包：`dist/macos-review/MagicDesk-I-Preview-734f5365-bedf93ef.app`，同名-arm64.zip为132,441,241字节。源为I_collar_clamps，保留完整月光、真实双谱表、薄片边缘法线修正与回声反馈。仍是独立口部预览，日常主App和完整海螺尚未更新。

## 新源的实际核对

重新跑了17开度×5膜片行程的组合、八个悬架幅值的实际Blender→GLB顶点对照、六张薄片的参考属性和33点真实开口测量。新开口结果与旧源一致，来自重新测量；GLB顶点最大对照误差约3.41e-8。光学组件在九个新源姿态下检查，只有原有安装销对内衬的设计接触，没有新增接触。

统一响应、取消/重开/再次按下、全三乐章包络响应以及当前音乐控制均按新组件回归；旧组件的音乐默认配置也回归。检查文件位于 `review/I_refinement/part_a_mouth/shutter_r2/collar_clamps/`，各自范围写在结果中，不能替代艺术、连续碰撞或全曲听验。

## 配置与打包

`tools/bind_i_collar_preview.py`在新源检查通过后生成独立的 `art/I/collar_clamps/aperture_profile.json` 和 `music_layout.json`。源报告明确指定这两个配置以及本组件的检查路径；查看器和谱面控制读取所选配置，旧文件保留。没有把旧passed或旧源哈希直接换名冒充新检查。

相关检查脚本支持传入报告位置，结果按组件存放。构建哈希现在覆盖生成的project.godot、导出配置和完整查看器模型定义，避免只改模型配置时仍沿用旧包名。

## 实际原生证据

`native/native_check.json`记录：新卡扣外观可见；E触发一次发出/返回；点击投射器播放；点谱面在290.296476秒暂停，时钟/谱位保持且膜片归位；恢复播放后点击口沿，声音/谱面收束并合拢。重新开启后留在约0.007934秒的带谱暂停状态。旧e54f5593窗口正常退出，包文件保留。

这些是限定的原生检查，不是完整16分钟原生听验、精准谱位、峰值VFX观感、性能或AAA验收。卡扣维护松开仍只在独立机构演示中，普通播放不松开固定卡扣。

## 下一项

A以当前可工作的组合候选进入B配合研究。先在真实共享底座上确定安装姿态和整体尺寸，再以原选图恢复B的饱满螺形与逐片开启，随后C连续内芯；A细节随整机继续收敛。不能把放大的独立查看器当作主App正常尺寸验收。

入口 `production/I_refinement/part_b_shell/FITTING_BRIEF.md`；实测接口与投射器边界在 `review/I_refinement/part_b_shell/mouth_attachment_reference.json`。
