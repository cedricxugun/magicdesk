# 前端导向与归位槽

先补内置生图 `guide_return_detail_r1.png`，保存提示词 `PROMPT.md` 和来源 `art_manifest.json`，再从已保留的三卡匣源独立制作 `I_front_guides.blend` / `I_front_guides.glb`。图只约束工艺/结构表达；真实尺寸与配合来自源模型、实际内衬射线和动作曲面。

## 实际制作

- 六只导轮轴承与短桥式镍支座，曲面脚贴合内衬轮廓，以真实径向螺钉/孔连接；不再悬置导轮端面。
- 连续轴穿过两端轴座，导轮套筒/肩部/挡边围绕它转动。转动直接取同一片的卷收进度，源动画与运行时都能反向/取消。
- 三只归位槽沿原叶片末端路径布置，正面开口、后方托底，带深色内衬、圆角和后端限位；保留叶片真实厚度及全部形变数据。
- 首轮槽底太浅；同状态近景又发现开口朝后。这两项分别通过曲面间隙和朝向修正，不能将早期图当最终观察。
- 首组导轮旧挡边碰到安装座，挡边按当前空间收小；孔位布尔的重合接缝需焊合后再核对封闭性。所有源/组件检查必须与最新 build.json 对齐。

## 来源与检查

- `blender/collection/build_i_front_guides.py`，复用无清场/保存副作用的 `i_machined_geometry.py`。
- `review/I_refinement/part_a_mouth/shutter_r2/front_guides/build.json`：准确源/组件哈希、导轮、归位槽和内衬安装信息。
- 同目录 `geometry_check.json`：源动作采样、实际薄片表面/自身接触及网格封闭性。
- `hardware_check.json`：包含新增旋转导轮与原固定结构之间的接触；保留原始刚性装配记录，另列独立运动组接触。
- `source_poses.json` / `runtime_pose_qa.json`：从实际 Blender 求值姿态独立记录，再与导出的 Godot 层级/驱动对照，包含往返、反向及重复进度。此项为无窗口的几何验证，不是原生手势验收。
- `runtime/`：实际 Metal 固定相机与导轮、入槽、离槽近景。相机位于口部内侧，避免用被瓷口沿挡住的画面冒充部件检查。

当前仍是独立 A 部件候选。槽体、连接、材质与整体演出需实际美术对照；连续碰撞、工程可制造性、完整海螺及主 App 验收尚未完成。没有替换用户收到的 `MagicDesk-I-Preview-777613d0`。接下来继续 A 工艺/材质/膜片声学表现，再依既定 B 螺壳、C 内芯、完整主体与月光模块推进。
