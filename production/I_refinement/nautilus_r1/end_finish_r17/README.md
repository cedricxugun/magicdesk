# 后法兰收口的折边与膜片捕获

当前有 `partial/` 实验源/组件，并非整组完成。a595b436的局部检查通过，但实际Metal近景显示压边碎、尖、覆盖不完整，见对应`visual_assessment.json`，不能以这些检查替代美术。后续候选继续尝试真实局部圆角、源拓扑交点和渲染三角清理，仍有未建面带；最新partial文件也未被整体视觉接受。可靠结构接续是R16b的1ebb1626，未包含这些包边实验。

已进一步定位09面带问题：原多边形网格的边连通检查通过，实际`loop_triangles`却产生两对反向相同三角面。此修复已经独立合入R16b，其他9814个渲染三角面实测保持。源裁切交点身份、角点预检和“实际三角面”检查都应保留；直接法线外偏移仍不足以完成复杂折返处的成型护边，需要独立护边构造与视觉复核。

设计图 `formed_rims_and_membrane_capture_r1.png` 使用内置 image_gen，原提示词为 `PROMPT.txt`，来源清单为 `art_manifest.json`。参考成型镍质U折边、深色膜片压边和局部金属扣持。图只确定工艺语言，实际轮廓仍按R16源，不扩大底座、改口部或照抄图中被放大的压边。

初始来源为 `I_nautilus_throat_terminations_r16.blend / f88bcfa5…`；其后可靠源为R16b。`build_i_end_rims_r17.py` 尝试从真实端面及邻接面带生成内外两层折边，在08窄折返与09面带上发现问题。`--preflight`输出整批诊断；`--partial`只生成满足当前构造条件的段，并显式保留未完成段，不能称整组完成。不可为了得到文件继续放宽间隙/角点限制。

已排除两类算法错误：按距离收集法线会混进薄端的另一侧面；对近乎平行的三角面强制同距，会产生无意义的切向长角。现在用拓扑相邻的原始面平面，并以有上下界的凸约束求外偏移；另试了只在真实收口急凸边倒圆。仍有一个窄折返无法在当前有限厚度和长度范围内直接包覆。

诊断在 `review/I_refinement/nautilus_r1/end_finish_r17/corner_failure.json` 及其指向的独立blend；包含原面带、失败点标记、实际法线和检查范围，场景中还有检查包络，不是正式模型。位置约Blender `(-0.72958, 0.43142, 1.87852)`，属于08框架上方收口。旧失败日志保留。R16保持不动。

下一步需改变该处的包边构造：建立可制造的独立成型护边，在小折返处设置圆角或跨过局部凹折，沿主要可见轮廓和接触带定位，再核对对基材/后法兰的实际间隙。不能把整个凹折的所有三角面都当作等距压模继续求长尖角。先在这一段验证截面与轮廓，再推广08/09端部及膜片压边，不因图稿或单项检查称AAA。04夹座、底部支承路线、上甲实机构、全月光/材料/原生仍未完成。
