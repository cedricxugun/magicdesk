> R46更新：像素射线只证明部位归属，不证明每个采样位置都存在坏曲率。深截面在.3–.4间由一环分成两环；R45直接深处放样破坏结构并与06重叠，未采用。下一项是R47原边界内的01均匀网格对照，不能把R44推测当已证明的折痕原因。

# R44 可见壳面归属诊断（没有改形体）

基于当前R40源8767879e的源帧121，使用整机review相机的像素射线。只是原源帧诊断；尚未独立证明该帧与运行时所有机构姿态一致，不能直接当几何/美术验收。

记录在 review/I_refinement/nautilus_r1/cowl_surface_probe_r44/pixel_ownership_rest_depth.json。整机左上大片归属IN1_PorcelainPanel_05：近前沿采样的复位轴向深度约.050/.086，尖部连接附近.317。前下大片主要归属IN1_PorcelainPanel_01：复位深度约.439/.446/.589；不要误当全部是FixedMouthCheek05。

早期clean_cowl重建只覆盖深度<=.30。前下可见缺陷有一部分在保留的旧曲面上，不能只继续调新前沿圆角或深度<=.30的Bezier控制点。下一轮先比较01/05实际曲面与连接区，再决定更深真实截面或局部重构；当前没有新形体候选。

若复用build_i_clean_cowl_r23.py，必须先改为配置当前来源、保留R36的形变导出（旧脚本export_morph=False会丢掉它）、全源保护、当前导光UV/材质和R43透明谱面/镜片附件组合。不可直接运行早期默认脚本覆盖当前源。共有底座、A与机构固定不变。
