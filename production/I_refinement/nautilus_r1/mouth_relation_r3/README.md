# 口部与卷壳关系纠偏

2026-09-13 用户明确否定：最前面圆口的朝向、位置、与身子的连接关系都与效果图不符。

艺术目标仍为 `production/I_refinement/nautilus_r1/nautilus_states_centered_r2.png`。REST 中口部位于卷壳左下侧，圆口斜向侧前方，外侧卷壳以饱满曲面接续到口沿；OPEN 仍保留这条固定下侧承接体量。当前候选却把较正面的圆盘贴在扁圆主体前方，短环形肩套与独立圆盘形成深缝。仅旋转圆盘不够。

先做独立的连接形体修正，固定底座/相机对比正、侧、顶和展示视角，并保留旧源。需要重新核对口部实际法线、口心相对卷心位置、前后厚度、接续曲面的切向与口内通道。目标图不提供精确 3D 尺寸，不能伪造还原率或相信图中数字。

整个 A 子树、居中谱面和三条扫描光路使用同一安装变换，不能只移动可见瓷口。新的壳面/孔位会使旧配合与扫掠证据失效，重新建立之后再接入四组机构和完整演出。不新增用户审批阶段。

保留四组实际导向源：`blender/collection/I_nautilus_cassettes_r2.blend`，SHA256 `3024d92ea8064f044c80fbe2791afff289eecf2c3fe4412fa4f37e2e9d967b4c`。其拓扑/外部扫掠/内部接触报告只属于该被否定形体。动力图尚未实现，新原生包未导出。

状态：口部连接形体修正中，未锁形、未用户验收。

最新源 **d84b7d87…**，组件 **6071bffc…**，对应 `checkpoint.json`。新源 9 姿态外部扫掠所查项已通过；整体拓扑仍 false，原因是若干内腔及固定壳网格岛仍待建立真实接头。不能以这轮通过的范围宣称全部结构/动作完成。

## 当前独立模型

制作源 `blender/collection/I_nautilus_mouth_relation_r3.blend`，组件 `app/assets/collection/components/I_nautilus_mouth_relation_r3.glb`，精确哈希和证据以 `review/I_refinement/nautilus_r1/mouth_relation_r3/build.json` 为准。构建入口是原形体生成器的显式 `--shape-config=production/I_refinement/nautilus_r1/mouth_relation_r3/shape_config.json` 分支；没有重跑覆盖旧源，配置模式禁止输出到旧 `I_nautilus_form_r1.blend`。

安装原点由 Blender `(-0.52,-0.52,1.61)` 调为 `(-0.87,-0.08,1.58)`，向外法线由归一化 `(-0.34,-0.935,0.10)` 调为归一化 `(-0.80,-0.595,-0.07)`；A 整体仍等比 0.70。以上是实际候选安装参数，不是从效果图读出的精确尺寸，也不是可见唇缘中心。主体的半厚参数由 0.59 改为 0.67，底座未改。

新增 `i_nautilus_formed_throat.py`：从真实壳片截面共享顶点延伸到口后，内外壁身份通过原网格层数据传递；先留出 A 的轴向占用空间，再建立曲面。旧的圆盘短肩套不能独自承担这个连接。低部/前部壳固定，上方三片仍为运动研究，旧四组实际机构源保留，但新源尚未重新装配对应实体机构。

同相机渲染包括原来的正/侧/顶/背视和 hero，以及新旧共用的近似参照视角 `relation_rest`。后者相机为 Godot `(-3.0,3.55,7.70)`，目标 `(0,1.66,0)`，32 度视场；是近似对齐，不是相机标定。真实底座范围和源哈希写在 `views/relation_review.json`。`relation_music` 仅静态谱面组合，完整月光仍是独立音乐模块。

## 本轮诊断与保留的限制

- 单独旋转/平移 A 的第一版仍像短接头，已保留在历史源；随后建立壳片本身的连续延伸。
- 大块铜色“破洞”经隐藏内腔的对照确认主要是旧铜环穿出新外壳，不能只归咎于切孔。现将前端实网格截到新喉道后方并封实截面，完整组合图重新显示所有剩余内腔件。
- `relation_cowl_isolation` **明确隐藏了内腔，仅用于诊断**，不能作为整机完成图。完整图是 `relation_rest` / `relation_open`。
- 旧边沿倒角在新薄曲面上制造破边，本阶段取消该自动倒角；外壳边缘工艺尚未重新精修。
- 内腔仍有被新喉道分开的网格岛，末端承座/连续喉管/承力连接没有因此完成。`topology_check.json` 的整体结果仍不可视作通过，不能靠拆名称或删除可见件刷绿。
- 开合检查针对实际相交三角面给出空间范围。当前将05在 Blender z=1.70附近的下颊分成固定实体，留0.004的分件间隙，上盖恢复短程抬离；本源9姿态所查项通过。它不覆盖全 A 连续动作、光学、所有固定件、体积包含、真实交互或性能。
- 新斜向相机下 `relation_music` 的谱面显得偏前左、金色扫描偏弱；局部坐标上的居中不能直接当作新整机视角的可读性验收。下一轮连实际投射深度、触须光路与完整音频一起查。
- 未锁形、未达到最终材质与动作标准、未替换 App；不得沿旧四机构的绿色报告直接打包这版。
