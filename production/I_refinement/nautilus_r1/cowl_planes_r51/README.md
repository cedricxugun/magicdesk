# R51 平面收口与曲面法线修正：当前接续候选

当前源 `blender/collection/I_nautilus_cowl_planes_r51.blend`，SHA **8f0151799d1b827806c8489f6d86d2b7036dd97b255a5629d857bfb698180bbc**。实际组合 `review/I_refinement/nautilus_r1/cowl_planes_r51/build.json`，精确组件、配置及媒体哈希见 checkpoint.json。来自 R40/R43 的原几何，不采用 R45/47/49 的重建形体。

R43 三镜片、真实无黑底谱面、月光与 R40 导光保持；真实 HELIOS 底座保持。主 App 未更新，全目标 active，未做 AAA 或用户美术验收。

## 实际改动和看到的效果

四个壳面（01、05、06、固定下颊05）采用面积/角度加权法线。对有明确平面的口沿封面和固定下颊切面，分离硬边并写入对应平面法线，避免极小三角面的不稳定法线参与曲面插值。

看过同相机实际近景：平整收口上缘的锯齿状高光在 R51 消失，部分扇形明暗减轻。**弯曲拼接处仍有尖折、裸边与残余扇形变化，几何没有因此修好。** 不把阴影/SSAO关闭图当交付；当前正常图和视频都保留实际阴影与环境遮蔽。

- 修改前近景：`review/I_refinement/nautilus_r1/cowl_shading_r48/macro/views/cowl_baseline.png`
- 修改后近景：`review/I_refinement/nautilus_r1/cowl_planes_r51/macro/views/cowl_baseline.png`
- 整机：同目录 `views/relation_open.png` / `relation_rest.png`
- 新视频：`take_r1/cowl_finish_r51.mp4`，实际750帧/25秒，含月光片段、暂停/恢复/透明谱面与合壳。

本轮沿用既有 R21 局部工艺图，没有新增生图或外部 API。法线是当前网格的制作数据，未用更换形体或去掉阴影掩盖问题。

## 保留证据

`geometry_preservation.json` 确认所有源位置、实际三角索引、UV、材料分配、变换和形变坐标保持；只改变指定壳面的着色法线、平面光滑标志和收口硬边。

`runtime_geometry_qa.json`：750个实际导入网格、1,739,968个有向三角坐标，多重集合按float32字节精确相等，朝向和重复数保持；世界变换逐标量相等。法线变化只出现在四个指定壳面。最初按三角排列顺序比较失败，经 `cowl_weighted_r50/triangle_order_diagnostic.json` 确认只是顺序变化，四件的新增/缺失有向三角均0；不是忽略不同几何。

`light_qa.json`：12个实际导入膜片顶点/法线/索引及形变缓冲仍与 R36 相同，原录音输入下轨迹差0，当前12导光组状态、关闭和释放检查通过。`take_r1/validation.json`：实际帧数、音频抽查、暂停静音、时钟与最终闭合帧一致性通过；不代表完整三乐章逐音同步或原生交互验收。

## 本轮排除的方向

- R45 / 8b423278：01直接重建至深度.62，破坏原来从一环分成两环的边界，与06重叠且画面产生台阶，拒绝。
- R47 / d07c618c：01在原边界内均匀网格，没有明显视觉收益，并有一处微小源接触，不采用。
- R49 / 6379c4ca：06在原边界内均匀网格，自接触/九姿态/口部与导入膜片检查通过，但目标纹理仍在，不以通过数接续。
- R48：实际对象着色确认窄条主要是06，后方大面是01，固定下颊另有自己的平面。实际渲染射线表明窄条样本大多仍在深度.07–.16；此前 R44 大片源射线不能定位窄条缺陷。关阴影/SSAO后仍有扇形/锯齿，故继续查几何/法线。
- R50 / ebf92187：加权法线改善部分扇形，但平面上缘仍有锯齿；R51补了明确平面边界，不直接用R50。

下一步处理可见弯曲接合和完整金属收边，先明确坏边实际对象/三角和边界拓扑，保留现有正常着色与动作。不得直接深截面放样穿过分叉、不得沿旧默认脚本导出丢失膜片形变。旧07基线、供能/动力、全曲校准、原生交互/性能、F后续与J/K/L/M/N及最后G仍未完成。
