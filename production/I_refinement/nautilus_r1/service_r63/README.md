# R63 拆解分镜与03号机构首段动画

**完整拆解尚未接入主App。** 当前可靠整机仍R61源0ba13905，R60灯光/谱面和R62共用全息控制保持。本轮新增分镜、真实接口/路径诊断、独立03号退销/退出动画资产和审阅驱动。全目标active。

## 设计与反复验证

使用内置 image_gen，分镜为 `service_storyboard_r1.png`，完整提示词 `PROMPT.txt` 与来源记录 `art_manifest.json`。以当前R61关闭/展开截图约束形体，遵循原子之心方向。图只指导解锁→分层检修→反向回装的阅读顺序；图中新长传动轴和若干连接件不是现有模型，未照搬，不能凭图说工程结构完成。

实际路径读取Blender源三角面和层级，不从图上猜数字。失败版本均保留在review/service_r63：

- 03完整前后壳/气缸整组径向下移，会撞低鞍座、支承座和底脚；改横移又穿过声学框/膜片。
- 把前罩带活动导向架一起抽出，活塞密封/活塞杆会撞气缸筒体，不能为通过而把这些障碍删掉。
- 先退销再让罩体斜向退出，连接舌仍擦到叉架/轴套。现在先沿实际叉架开口方向退出.12，再侧向展开；原气缸、导杆/叉架与后壳保留。
- 口部整体轴向退出仍会撞 `IC1_CollarLower`，必须先处理夹环解锁。此问题未解决，不接回主App，也不宣布整机拆解通过。

## 当前可接续的03号阶段

采用 `stage03_wide`。动作顺序是负侧端盖退出→销轴和正侧端盖一起抽出→连接舌退出叉口→前罩和螺接连接舌侧向分离。只动5个真实节点根，子级真实螺钉、安装鞋和金属边随所属模块运动；其它总成及口部保持原位。没有重造筒体、删除固定件或把所有零件挤成一团。

初版.2侧移虽然机械采样通过，但画面仍遮住主体，不采用作展示；宽版后段侧移1.0。沿用相同前段退出路径，重新检查并扩大审阅画布宽度，**没有缩放底座或模型**。

- 动画源：`blender/collection/I_service03_motion_r63b.blend`，SHA c5ae9595c816a168ae146b644ddc61237de194e84afbdf28787894ef56c975e5。
- 动画资产：`app/assets/collection/art/I/service_r63/service03_motion_wide.glb` 与同名JSON，3.5秒/60fps。它来自Blender真实关键帧与求值标记的烘焙，不是只在Godot里写几组位移。
- **这个.blend只作为03检修动画源，不可替换R61整机源。** 它以普通开壳位置为准备态，原操作动作已在副本中冻结；主模型几何仍来自R61组件。
- 审阅驱动 `app/review/i_service03_driver.gd` 要求源/组件签名及实际准备姿态匹配，使用导入动画，时间游标可平滑反向和取消；尚非主App完整拆解控制器。

## 证据与限制

`stage03_wide/plan.json`：24个采样时刻，口部和其他罩体保持原位，03退出路径无新增所查相交。初始端盖/销轴有18对既有接触，报告保留，不视为无接触或豁免；离开后所查时刻清除。不能推断全部连续时间、包含关系、螺纹力学或完整拆装已验收。

`runtime_qa.json`：实际R61导入模型与Blender独立目标标志点对比、整机展示旋转后的坐标一致、途中反向、完整回装以及局部矩阵逐标量回到准备态通过。**回到准备态是普通开壳位置，不是整机合壳。**

Godot默认将三轨211帧压到46/16/20关键帧，使等待段提前漂移、最大误差约.000602。原失败证据在stage03/automatic_optimized_pose_failure.json。已按实际AnimationPlayer导入设置关闭optimizer/compression并用60fps，3轨各211帧，初版独立源对照误差约1.33e−7，当前宽版约1.69e−7。阈值未放宽。官方实现参考：https://raw.githubusercontent.com/godotengine/godot/master/editor/import/3d/resource_importer_scene.cpp （动画节点optimizer/enabled）。

实际展示 `stage03_wide/render/service03_r63b.mp4`：1200×900，480帧/16秒、无音乐，含两次途中方向变更和最终回装；源首末PNG逐像素相同。影片从普通开壳准备态开始，只展示03号阶段，已明确标注非整机完成。宽版已看静帧，部件与主体有可读间距；仍没有最终光学接触特效或原生输入验收。

## 下一步

先继续口部夹环及其真实紧固件释放。`IC1_SeamCrossBolt`曾把两端帽布尔并为一个网格，不能直接整根穿过夹环，要按实际接合位置建可分离界面并验证。03后壳/固定缸体随后拆，不可把它和已脱离的转舌同时放入重叠层级，当前审阅驱动禁止祖先/子级重复驱动。04/05整组路线只有初步诊断，仍缺完整紧固/供能断开和全程检验。

再整合服务准备、退销、各模块分离、反向/取消、返回正常开壳与收谱合壳、切换退出。保留全曲与准确谱时校准、侧边回折/内衬、整体材质工艺、性能与F/I/J/K/L/M/N/最后G全队列；无新安装包，无Git推送，不称AAA完成。
