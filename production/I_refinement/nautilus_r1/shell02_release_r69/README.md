# R69 02号下壳释放依赖诊断

源是当前开发R68 **6eaf5bb6**，没有制作新模型或动画。`probe_i_shell02_release_r69.py`在R68分层支柱/穿口终态保持所有已退出件和真实底座，选择实际02号carrier的四个网格：瓷壳和三段原边条，按父子层级写全局位姿避免重复位移。

五个直线路径均失败，所有接触保留在 `review/I_refinement/nautilus_r1/shell02_release_r69/probe.json`：

- 向下：移动 .006 后先触及 `IN1_FixedRearShell_03`，8对三角面；更远会撞暂存支柱/穿口与底座。
- 向后下：移动 .012 后先触及同一03后壳，36对，继续深入会碰声学层。
- 向左下/向前下：初始 .003 即与 `IC1_CollarLower`、`IN3_ContinuousThroat` 等接触；向前下也碰上夹环。
- 向右下： .006 时与上夹环和03后壳接触。

初始装配采样为0接触，失败不是沿用初始穿插。没有删除固定件、忽略新碰撞或把静态终点当完整拆解。

**下一步先处理03固定后壳的真实固定/释放依赖**，承接R63前罩/转舌已经退出的首段，再评估02壳路线；不能直接跳过后壳拉02，也不能直接穿过夹环拉出口部。暂存件需要真实安全摆放，不能仅让它们退出后成为下一件路上的障碍。

这轮是五条平移路线诊断，不证明所有转动/组合路径不可能，也不是全拆解动作、结构工艺或AAA验收。全模型目标保持，主App仍R68，无新安装包。
