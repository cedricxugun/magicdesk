# HELIOS · 孵日器 B

双击 `dist/HELIOS.exe` 启动。软件直接显示可交互的三维模型，底座保持静止，上部默认旋转。运行不需要安装 Blender 或 Godot。首次启动会将内置渲染程序释放到当前用户的本地应用数据目录，随后出现桌面摆件。

| 底座按钮（从左至右） | 作用 | 快捷键 |
| --- | --- | --- |
| 纵向开关 | 唤醒 / 休眠；休眠会收拢机构 | 1 |
| 太阳 | 六瓣同步绽放 / 闭合 | 2 |
| 三圆 | 核心过载、转环加速与放电 | 3 |
| 四圆 | 70 组真实组件分层拆解并悬停 | 4 |
| 三角 | 从当前位置重新组装，可中途打断拆解 | 5 |
| 星芒 | 上部展示旋转 / 暂停 | 6 / 空格 |
| 红色 X | 收拢、熄灯并关闭应用 | 7 |

拖动底座可以移动桌面摆件，拖动上部可以手动旋转。右键或 Esc 打开静音、置顶、重置位置和退出菜单。按钮悬停时会显示当前功能提示。点击第7枚红色X会先结束过载、将零件收拢并逐渐熄灯，约3.4秒后关闭。

开合不会缩放模型。固定的三维镜头在独立渲染视口中工作，桌面显示使用 Windows 原生逐像素透明合成。模型实时生成的颜色和透明度直接决定桌面轮廓，透明处不接收鼠标，花瓣之间的空隙也可以点到后面的桌面。位置、画面与裁切范围一次提交，不在按钮动作中重建显示窗口。

材质使用光滑釉面陶瓷、镍合金、深色钢、红色漆和局部发光。实时渲染采用 Forward+、面光源、摄影棚 HDR 环境、零件局部遮蔽和多重采样抗锯齿。红色核心、内侧导光槽、外壳标志条和底座供能灯随状态变化；打开后的飞行亮粒使用大范围椭圆轨迹，放电连接核心与机械触点。

开蛋约3.5秒：先泄压，再展开花瓣、抬升并点亮核心。环形高压蒸汽使用 Blender Mantaflow 模拟的三维密度缓存，向下撞击虚拟地面后铺开、卷起；仅从闭合状态打开时触发。过载不会再次从地下喷气。待机时阀门小流量逸散，尾迹保留旋转历史，关闭后逐渐消散。靠近核心的蒸汽接受红光，远处保持中性白。实时画面与 Blender Cycles 离线渲染仍存在多次反射和间接光的差异。

## 可编辑文件

- `blender/Helios_Incubator.blend`：模型、材质、分件层级、600 帧完整演示动画（24 fps）和摄影棚灯光。
- `app/`：Godot 4.7.1 项目、交互逻辑、特效与导出配置。
- `native/`：原生透明桌面显示、输入桥接和单 EXE 打包源码。
- `app/assets/helios_model.glb`：带材质和分件层级的实时资产。
- `app/assets/mechanism.json`：机构姿态、分件偏移及按钮对应关系。
- `review/`：实际渲染与运行截图。
- `tests/`：功能验证与模型检查报告。

`tools/build_release.ps1` 会先导出内置渲染程序，再编译包含它的单文件 `dist/HELIOS.exe`。无需联网运行；原生显示与渲染进程仅通过本机回环连接交换画面和按钮输入。

打开 Blender 文件后，空格播放完整演示；也可运行 `blender/review_live.py`，在侧栏 HELIOS 中选择闭合、绽放或拆解状态。

摄影棚 HDR 来自 Poly Haven 的 Studio Small 09（Sergej Majboroda，CC0）。来源和许可见 `app/assets/THIRD_PARTY.txt`。模型、表面贴图、图标及音效为本项目制作。
